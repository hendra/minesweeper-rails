<%= render @board %>

<div>
  <%= link_to "Edit this board", edit_board_path(@board) %> |
  <%= link_to "Back to boards", boards_path %>

  <%= button_to 'Delete', @board, data: { "turbo-method": :delete, "turbo-confirm": 'Are you sure?' }, class: 'btn btn-outline-danger' %>
</div>
